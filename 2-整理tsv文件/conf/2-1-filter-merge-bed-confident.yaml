# =====================================================================
# 2 filter merge bed confident — configuration
# 所有相对路径均相对于 project.base_dir
# =====================================================================

project:
  base_dir: "/mnt/f/Onedrive/文档（科研）/脚本/Download/15-NUMTs-detector-V2/2-整理tsv文件"  # 2-整理tsv文件项目根目录

# --------------------------------------------------------------------- #
# 路径配置：严格阈值断点表、merge_bed 输入、confident merge_bed 输出
# --------------------------------------------------------------------- #
paths:
  python_dir: "python"  # Python 脚本目录
  log_dir: "log"  # shell wrapper 日志目录
  confident_gz: "/mnt/l/20-NUMTs/1-原始结果/data/3-SLE/3-严格阈值-排除参考NUMTs/1-所有样本/3-confident_breakpoints.tsv.gz"  # confident breakpoints 输入
  merge_bed_gz: "/mnt/l/20-NUMTs/1-原始结果/data/3-SLE/3-严格阈值-排除参考NUMTs/1-所有样本/8-merge_bed.tsv.gz"  # merge_bed 输入
  output_gz: "/mnt/l/20-NUMTs/1-原始结果/data/3-SLE/3-严格阈值-排除参考NUMTs/1-所有样本/9-merge_bed_confident.tsv.gz"  # 过滤后输出

# --------------------------------------------------------------------- #
# 工具配置：统一管理解释器
# --------------------------------------------------------------------- #
tools:
  python_bin: "python3"  # Python 解释器

# --------------------------------------------------------------------- #
# 运行参数：输出覆盖策略
# --------------------------------------------------------------------- #
runtime:
  overwrite: false  # 是否覆盖已有输出；命令行 --overwrite 可临时覆盖为 true
